# ============================================================================
# IRIS E2E Monkey Test - Environment Variables
# ============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env.test
# 2. Fill in your LOCAL test credentials (NOT production!)
# 3. The test will automatically load these variables
#
# SECURITY NOTICE:
# - ONLY use credentials for localhost/dev environment
# - NEVER commit .env.test to git (it's in .gitignore)
# - Tests will abort if BASE_URL is not localhost
# ============================================================================

# Frontend URL (must be localhost)
BASE_URL=http://localhost:3000

# Backend API URL (must be localhost)
BACKEND_URL=http://localhost:8080

# Test user credentials (optional - for authenticated tests)
# If not provided, test will continue as guest
E2E_EMAIL=test@example.com
E2E_PASSWORD=TestPassword123!

# ============================================================================
# NOTES:
# - Create a dedicated test user in your local database for these credentials
# - Make sure the backend is running on BACKEND_URL before running tests
# - Make sure the frontend is running on BASE_URL before running tests
# ============================================================================
