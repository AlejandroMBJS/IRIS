# This file serves as an example for the .env file
# Copy this file to .env and fill in the appropriate values.

# Server Configuration
SERVER_PORT=8080
ENVIRONMENT=development # Options: development, production, testing

# Database Configuration (REQUIRED for production)
# Example for PostgreSQL: DATABASE_URL="host=localhost user=user password=password dbname=dbname port=5432 sslmode=disable"
# Example for MySQL: DATABASE_URL="user:password@tcp(127.0.0.1:3306)/dbname?charset=utf8mb4&parseTime=True&loc=Local"
DB_DRIVER=
DATABASE_URL=

# JWT Configuration (REQUIRED for production)
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRATION_HOURS=24
JWTRefreshHours=168

# Security
BCRYPT_COST=12

# Logging
LOG_LEVEL=info # Options: debug, info, warn, error

# CORS (REQUIRED for production, specify frontend domain(s))
# Example: CORS_ALLOWED_ORIGINS="http://localhost:3000,https://your-production-frontend.com"
CORS_ALLOWED_ORIGINS=*

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# Payroll specific
DEFAULT_PAYROLL_CURRENCY=MXN

# Email Configuration (Optional)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=your_smtp_username
# SMTP_PASSWORD=your_smtp_password
# EMAIL_FROM=noreply@example.com

# Vault Configuration (Optional - if using Hashicorp Vault for secrets)
# VAULT_ADDR=http://127.0.0.1:8200
# VAULT_TOKEN=your_vault_token
# VAULT_SECRET_PATH=secret/data/iris-payroll
